calc input = do
    result = 0

    for character in input do
		result = match character where
        	| "+" -> result + 1,
        	| "-" -> result - 1,
        	| "*" -> result * 2,
        	| "/" -> result / 2
	end
end

print calc "++*++/"
